%h1 Approved Blogs
%table
	%thead
		%tr
			%th Title
			%th Description
			%th
	%tbody
		- @blogs.each do |blog|
			= content_tag_for(:tr, blog) do
				%td= blog.title
				%td= blog.description
				%td= link_to 'Show', blog_path(blog.id)